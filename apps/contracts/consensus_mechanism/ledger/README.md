This directory is used by `scripts/local_dev_chain.sh`.

The script generates `init-args.did` at runtime with the current local
identity account IDs so the local ICP ledger can mint test balances on the
development network.
